# Caddy 自动申请并续期 HTTPS 证书(切换 DNS 到服务器后再启动 Caddy)

youshoubei.cn, www.youshoubei.cn {
    encode zstd gzip
    # 强制后续访问走 HTTPS,防止首访/中间人被降级到 HTTP
    header Strict-Transport-Security "max-age=31536000"
    reverse_proxy 127.0.0.1:3000
}
