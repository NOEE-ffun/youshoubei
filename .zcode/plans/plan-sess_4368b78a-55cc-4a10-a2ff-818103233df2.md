# 稳健重构计划(阶段一清理 + 阶段二收敛 + 阶段三解耦)

三路审计结论:约 760+ 行死代码、多套重复实现、bracket↔canvas-editor 循环依赖、renderHeader 全量重建。关键删除依据(renderPlayerLibrary 不可达、bracket-model.js 无页面加载、CSS 死类零引用、deck-modal readOnly 死链)已逐项 grep 复核属实。

## 全局约束
- **行为零变化**:UI 文案、DOM 结构、交互、API 契约全部保持;本计划只删死码、去重、解耦
- 每个删除项动手前先 grep 复验零引用;每任务结束 `npm test` 必须全绿
- 每任务独立提交(`refactor: ...`);阶段结束跑手工冒烟:主页/赛程(编辑+比分+卡组弹窗)/选手库/海报(改字段+导出 PNG+OBS 源)/舞台页
- common.js 保持单文件(按你的选择,不做九模块拆分)

## 阶段一:死代码清理(约 -900 行,零风险)

**T1 common.js 死代码**
- 删不可达函数 `renderPlayerLibrary`(1098-1127)、`renderRosterEditor`(1129-1169):依赖的 `#player-library-list`/`#roster-editor-list` DOM id 全项目不存在(功能早已由 bracket.js openRoster 另实现)
- 删 `sidebarHidden`/`setSidebarHidden` 及 `LS_SIDEBAR` 常量(外部零引用)
- 导出面收窄:`TournamentUtils.statusBadgeMarkup` 移出导出(仅内部用);`TournamentApp` 移除 `migrateLocalToCloud/migrateCloudToLocal/openSettings/openManage/setActiveId/storageDelete/storageGetPlayers/isAdmin/uid` 挂载(均仅内部使用,函数本体保留)
- 修正失效注释(formatStartTime"主页共用"等)

**T2 删 bracket-model.js + test/bracket.test.js**
- 224 行生产零引用(无页面加载,server.js 主动屏蔽下载),仅测试消费;其能力已被 canvas-model.js 取代,且 AVATAR_COLORS 同名异值是"改错文件"陷阱
- package.json `test` 命令去掉 `node test/bracket.test.js &&`;README 测试段同步

**T3 bracket.js / canvas-editor.js 零散死项**
- bracket.js:`BracketActions.resetScores`(612-623,零调用)、`toggleEdit` 与 common.js:1670 的兜底 else-if、`playerRow` 未用第三参、未用解构 `debounce`
- canvas-editor.js:未绑定的 `onClick`(407-412)、导出的 `deleteCard`(零外部调用)、未用解构 `canEdit`/`errMsg`

**T4 deck-modal.js readOnly 死链**
- 唯一传 `readOnly:true` 的调用方 library.js 已删;bracket.js:606 不传 options。删 `readOnly` 状态、`canModify()` 及 6 处分支(约 15 行)

**T5 state.js 死代码**
- 删 `loadRoster`/`saveRoster`(生产零调用)、`packPayload`/`unpackPayload`(仅测试引用);vs-poster/test/run-tests.mjs 删对应测试段;api/poster-stage.js:37 过期注释修正

**T6 styles.css 死规则(~390 行,逐群 grep 复验后删)**
- 九群:`.brand`/`.main-nav` 旧导航(331-385)、`.page-wrap` 系(547-566 等)、`.rules-sidebar` 系(583-642)、`.roster-grid` 旧名单网格(678-734)、`.bracket-section` 旧竖排布局(735-780)、`.toolbar .spacer`、`.card-actions`(library 残留)、`.is-paused`、`.edit-sidebar` 系(2245-2265)、`.player-card-title*`/`.player-card-color*`(2532-2634,103 行)

**T7 vs-poster/css/style.css 死规则(~150 行)**
- `.topbar` 旧顶栏系(63-110)、`.theme-picker` 系(120-153)、`#poster-app .theme-menu` 系(155-204)、`.btn--primary`、`.kbd`、`.title-badge`、`.roster__del--armed`

**T8 仓库卫生**
- .gitignore 追加 `.impeccable/`、`.zcode/`、`.superpowers/`,并 `git rm -r --cached` 对应已提交文件
- 删 vercel.json(项目已迁自有 Node/ECS,与 server.js 的缓存头是重复真源)
- README 修正过期措辞(代码结构、测试清单)

## 阶段二:低风险收敛(机械替换)

**T9 画布几何常量统一到 canvas-model.js**
- 导出 `CARD_WIDTH/CARD_HEIGHT/COL_GAP/ROW_GAP/PORT_Y`(端口坐标 70/108)、默认画布尺寸
- bracket.js:10-13、270-272、302/409 的 40/24 字面量,canvas-editor.js:6-7、832 的裸 280、833 的 70/108 全部改引用——消除"改一处炸另一处"

**T10 api/ 公共化**
- 新建 `api/helpers.js`:`readBody(req, maxBytes)`(吸收三份复制的 chunk 收集+大小校验,连注释都是复制的)+ `sendJson`;data.js/upload.js/poster-stage.js 替换;顺手统一 poster-stage.js 的内联 res.status().json() 风格

**T11 common.js 内部去重**
- `compressImage`/`compressAvatar` 参数化合一(两份逐行对应的 canvas 重采样)
- IndexedDB 五连包装收敛为 `withStore(store, mode, fn)` 高阶函数
- `'ts:changed'`/`'ts:ready'` 提取常量(裸写 14 处);`typeof CanvasModel` 守卫 9 处收敛为 `hasCanvasModel()`;两处 `pad` 定义合一;quota 错误正则+文案提取常量

**T12 接入漏跑的 vs-poster 测试**
- package.json test 命令追加 `node vs-poster/test/run-tests.mjs`;先单独跑,31 个测试若不绿先修复再接入——消除"绿灯假象"

**T13 小 bug 修复**
- common.js `migrateLocalToCloud`(518-522):头像上传循环嵌错位于赛事循环内(功能侥幸正确),移出到外层
- `probeCloud`:无 AbortController 时 timer 仍设置的泄漏
- vs-poster stage.js `load()` 失败无限重试,加上限(10 次)
- api/oss.js:OSS 客户端模块级复用(当前每次读写新建)

**T14 vs-poster 工具收敛(明确分工不强行合并)**
- app.js `esc()` 兜底删除,直接用 `TournamentUtils.escapeHtml`;`notifyError` 桥接简化
- 在 `safeUrl` 与 `isAllowedURL` 处加注释说明 http: 策略差异的成因(主站图片入库需 https;海报编辑器本地转 dataURL),防止后续"统一"时改错

## 阶段三:关键解耦(行为保持,需手工回归)

**T15 renderHeader 增量化**
- 拆为 `buildHeaderSkeleton`(一次构建+一次事件绑定)+ `syncHeaderState`(只更新标题/状态徽章/按钮可见性);消除每次数据刷新全量重建 innerHTML——顺带修掉海报页头主题菜单/分辨率被重置的实际 bug

**T16 bracket↔canvas-editor 单向化**
- canvas-editor.js 的 8 处 `window.BracketRender.*` 反向调用改为初始化时注入回调;bracket.js→CanvasEditor 的 14 个方法收敛为窄接口;hint 文案与 `editing` class 归属 canvas-editor 单方;删除跨文件 DOM 内联样式传参(board.style.width/height → 显式参数)
- 回归清单:拖拽/框选/连线/微调四手势 + 新增卡/删除/粘贴

**T17 弹窗函数拆分(common.js 内部,不动文件结构)**
- `buildDialogs`(240 行)拆为 buildManageDialog/buildSettingsDialog;`renderManageList`(126 行)内 4 组业务(重命名/切换/复制重映射/删除)拆小函数

**T18 poster app.js bindFormControls 拆分**
- 198 行拆为 `bindSideControls('left'|'right')`(两侧×7 控件本就对称)+ `bindGlobalActions`;两段式 els 初始化合并到 boot

## 验证
- 每任务:`npm test` + grep 残留检查;每提交前冒烟对应页面
- 阶段三每任务额外:手工回归清单(见 T15/T16)
- 完成后总冒烟:五页面 + 云端模式(本地起服)完整走一遍编辑/保存/上传/导出/OBS

预计减量:阶段一约 -900 行(代码+样式),阶段二约 -100 行,阶段三为结构改善。批准后按 T1→T18 顺序执行,每任务一次提交。